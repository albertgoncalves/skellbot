#!/usr/bin/env bash

set -e

./build
response=$(
    curl "https://slack.com/api/rtm.connect?token=$SLACK_BOT_TOKEN" \
        | jq -r '.url' \
        | sed 's|^wss://||'
)
export RTMHOST=${response%%/*}
export RTMPATH=/${response#*/}
./src/Client
